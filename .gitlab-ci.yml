image: haskell:7.10.3

before_script:
    - while [ $? -ne 0 ]; do apt-get update -y && apt-get install -y -qq git-core dh-autoreconf; done

stack_job:
  cache:
    untracked: true
    paths:
      - .stack-work
      - ___restore
  script:
    - mkdir -p `pwd`/___restore
    - ln -s `pwd`/___restore /root/.stack
    - stack --no-terminal test rscoin
